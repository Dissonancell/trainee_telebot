🎥 Video Analytics Telegram Bot

Telegram-бот для аналитики видеоконтента, который использует локальную LLM (Llama 3.2 через Ollama) для преобразования вопросов на естественном языке в SQL-запросы к базе данных PostgreSQL.

Бот позволяет задавать аналитические вопросы в свободной форме и получать числовые ответы, вычисляемые напрямую на стороне БД.

✨ Возможности

🤖 Вопросы на естественном языке

🧠 Автоматическое преобразование текста в SQL

📊 Аналитика видео и динамики просмотров

⚡ Асинхронная работа с PostgreSQL

🔒 Локальная LLM без внешних API


🛠 Технологический стек

Python: >= 3.11
LLM: Llama 3.2 (ollama)
Telegram Bot: aiogram 3.x
Database: PostgreSQL
ORM: sqlalchemy 2.0 + asyncpg
Env config: python-dotenv
Package manager: uv

📂 Структура данных

videos
Таблица с основной информацией о видео:
id
creator_id
created_at
счётчики (просмотры, лайки и т.д.)

video_snapshots
Таблица с историческими срезами метрик:
временные значения просмотров
дельты показателей

🚀 Быстрый старт
1. Установка Ollama и модели

Установите Ollama
 и загрузите модель:

```bash
ollama run llama3.2
```

2. Установка зависимостей
```bash
uv sync
```

3. Конфигурация окружения
Создайте файл .env в корне проекта:
```env
TELEGRAM_TOKEN=your_telegram_bot_token
DB_URL=postgresql+asyncpg://postgresql+asyncpg://postgres:password@localhost:5432/database_name
MODEL_NAME=llama3.2:latest
```

4. Инициализация базы данных
Создание таблиц и загрузка данных из videos.json:
```bash
uv run python load_data.py
```

5. Запуск бота
```bash
uv run python bot.py
```

🧠 Примеры запросов
Сколько видео у креатора aca1061 вышло в ноябре 2025?
На сколько просмотров выросли все видео 28 ноября 2025?
Сколько всего видео есть в системе?

⚙️ Принцип работы
Пользователь задаёт вопрос в Telegram
LLM преобразует запрос в SQL
SQL выполняется в PostgreSQL
Бот возвращает числовой результат
Важные ограничения
Возвращается только одно число
При отсутствии данных или ошибке запроса возвращается 0
Все агрегации выполняются на стороне БД
(COUNT, SUM, работа с датами)
